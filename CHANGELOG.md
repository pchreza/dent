# تغییرات نسخهٔ آزمایشی

## نسخهٔ 0.6.0 — 2026-08-13

فاز پورتال بیمار تکمیل شد. با تأیید درخواست ثبت‌نام QR، حساب کاربری بیمار، نقش `patient`، membership کلینیک و پیوند `patient_user` به‌صورت اتمیک ساخته می‌شوند و بیمار پس از ورود نقش‌محور به پورتال مستقل خودش هدایت می‌شود.

پورتال بیمار read-only است و داشبورد، نوبت‌ها، طرح‌های درمان، فاکتورها و اعلان‌های Tenant-scoped را با رابط فارسی RTL، تاریخ شمسی، فونت آفلاین Vazirmatn و ایزوله‌سازی داده‌های LTR نمایش می‌دهد. بیمار به پنل کارکنان دسترسی ندارد، در اولین ورود ملزم به تغییر رمز اولیه است و اگر به چند کلینیک متصل باشد فقط بین membershipهای خودش می‌تواند Tenant فعال را انتخاب کند.

Middlewareهای `patient_portal` و `staff_portal` مسیرهای دسترسی را از هم جدا می‌کنند. کنترلرهای پورتال از Patient و Tenant فعال جاری استفاده می‌کنند و تست‌های Feature برای ورود، جداسازی داده، چندمستاجری، جلوگیری از ورود بیمار به پنل کارکنان و تغییر رمز اجباری اضافه شدند.

QA نهایی: ۴۳ تست و ۱۸۳ assertion موفق، Pint روی ۱۳۶ فایل، Composer validate/audit، Blade view cache، Vite build، `git diff --check`، بازبینی بصری دسکتاپ، responsive smoke authenticated در ۳۷۵ و ۷۶۸ پیکسل و آزمون Drawer شامل aria state، focus return، Tab loop و Escape موفق. گزارش‌ها در `docs/qa/patient-portal-visual-findings-fa.md` و `docs/qa/patient-portal-qa-checklist-fa.md` و طراحی فنی در `docs/product/phase-7-patient-portal-plan-fa.md` قرار دارند.

## نسخهٔ 0.5.0 — 2026-08-13

رابط کاربری کل پلتفرم با یک Design System مستقل، روشن و حرفه‌ای بازطراحی شد؛ سایدبار گروه‌بندی‌شده در سمت راست، هدر چسبان، breadcrumb، منوی کاربر، زمینهٔ کلینیک فعال، مسیر فعال پویا و Drawer موبایل به همهٔ صفحه‌های پنل اضافه شدند. آیکون‌های SVG مستقل، کارت‌ها، badgeها، فرم‌ها، جدول‌ها، empty stateها، اعلان‌ها و الگوهای پرونده/درمان از یک قرارداد بصری مشترک پیروی می‌کنند.

داشبورد از KPIهای نمایشی به شاخص‌های واقعی و Tenant-scoped بیمار، نوبت امروز، ماندهٔ فاکتورهای باز و نوبت‌های آینده ارتقا یافت. وضعیت‌های نوبت، فاکتور، بیمار و درخواست QR فارسی شدند. تاریخ‌های نمایشی در پرونده، فاکتور، اعلان، QR و داشبورد به تقویم شمسی منتقل شدند و مقادیر FDI، موبایل، مبلغ، ساعت و شناسه با `bdi`/`dir="ltr"` ایزوله شدند.

جریان‌های ورود، نصب، ثبت‌نام عمومی، بیمار، پرونده، وضعیت دندان مینیمال، طرح درمان، تقویم، نوبت، فاکتور، شعبه، کاربران، مراحل درمان، QR و اعلان‌ها با primitiveهای مشترک بازطراحی شدند. رابط موبایل در 375، 390 و 768 پیکسل از نظر overflow و Drawer بررسی شد؛ Drawer با focus return، aria state، Escape و Tab loop آزمون شد. پشتیبانی reduced-motion و contrast audit توکن‌های اصلی نیز اضافه شد.

QA نهایی: ۳۸ تست و ۱۵۳ assertion، Pint روی ۱۲۷ فایل، Composer validate/audit، Blade cache، Vite build، diff check، بازبینی بصری دسکتاپ و smoke responsive authenticated موفق. هشدار Vite دربارهٔ مسیر public فونت غیرمسدودکننده است و هر دو فایل Vazirmatn در `public/fonts` موجود هستند. چک‌لیست و یافته‌های بصری در `docs/qa/ui-redesign-rtl-checklist-fa.md` و `docs/qa/ui-redesign-visual-findings-fa.md` ثبت شده‌اند.

## نسخهٔ 0.4.0 — 2026-08-13

تجربهٔ قبلی نقشهٔ فک گرافیکی و پنل پیچیدهٔ روند درمان به‌طور کامل حذف شد. صفحهٔ جدید «وضعیت دندان‌ها» مینیمال و سریع است و آخرین وضعیت هر دندان را با نام آناتومیک فارسی، کد FDI، سطح، یادداشت، تاریخ شمسی و عملیات مستقیم نمایش می‌دهد.

فرم ثبت سریع در کنار فهرست قرار دارد و با انتخاب هر ردیف، دندان و سطح را از قبل انتخاب می‌کند. ایجاد طرح درمان برای هر دندان نیز با یک لینک مستقیم انجام می‌شود. جدول تاریخچهٔ کامل برای حفظ سوابق افزایشی باقی مانده است. JavaScript و CSS مربوط به اودونتوگرام قدیمی حذف شدند و فقط تعامل لازم برای افزودن آیتم‌های طرح درمان باقی ماند.

QA: بازبینی مستقیم RTL موفق؛ ۳۷ تست و ۱۴۰ assertion، Pint روی ۱۲۶ فایل، Composer validate/audit، Blade view cache، Vite build و `git diff --check` موفق.

## نسخهٔ 0.3.0 — 2026-08-13

نمودار دندان به اودونتوگرام حرفه‌ای مبتنی بر SVG بازطراحی شد. تصویر یکپارچهٔ فک بالا و پایین با لایهٔ استخوان، لثه، قوس دهانی و سیلوئت متفاوت دندان‌ها اضافه شد. در حالت پیش‌فرض فقط دندان‌های دارای رخداد یا آیتم طرح درمان نمایش داده می‌شوند و نمای کامل دندان‌های دائمی و شیری به‌صورت جداگانه قابل انتخاب است تا از هم‌پوشانی و شلوغی جلوگیری شود.

کدگذاری FDI با نام آناتومیک فارسی، ربع فک، نوع دندان و نشان LTR نمایش داده می‌شود. انتخاب دندان، URL، سطح، فرم ثبت سریع، پنل «داستان درمان دندان»، Timeline بالینی، Stepper وضعیت درمان و CTA ایجاد طرح درمان را بدون Refresh همگام می‌کند. فرم طرح درمان نیز کد دندان و سطح انتخاب‌شده را در آیتم نخست پیش‌انتخاب می‌کند. تاریخ‌های پنل و تاریخچه با تقویم شمسی نمایش داده می‌شوند.

QA: بازبینی مستقیم RTL و حالت‌های active-only، نمایش کامل دائمی و روند چندمرحله‌ای موفق؛ ۳۷ تست و ۱۳۸ assertion، Pint روی ۱۲۷ فایل، Composer validate/audit، Blade view cache، Vite build و `git diff --check` موفق.

## نسخهٔ 0.2.1 — 2026-08-12

نمودار دندان از شبکهٔ سادهٔ کارت‌ها به نمای گرافیکی فک بالا و پایین ارتقا یافت. دندان‌ها با SVG قابل‌فهم، کد FDI، رنگ وضعیت، نشانگر بالینی، legend فارسی و کارت‌های آماری نمایش داده می‌شوند. انتخاب دندان اکنون `aria-pressed`، بنر خلاصهٔ وضعیت و مقدار فرم ثبت را هم‌زمان به‌روزرسانی می‌کند و در موبایل به‌صورت هوشمند به فرم ثبت هدایت می‌شود. جدول تاریخچه به‌عنوان مسیر جایگزین قابل‌دسترس حفظ شد.

QA: بازبینی مستقیم RTL موفق، انتخاب تعاملی دندان ۱۶ موفق، ۳۴ تست و ۱۲۰ assertion، Pint روی ۱۲۵ فایل، View cache، Vite build و `git diff --check` موفق.

## نسخهٔ 0.2.0 — 2026-08-12

فاز پروندهٔ دندان‌پزشکی پیشرفته تکمیل شد. فیلدهای سفارشی Tenant-scoped با نوع‌های متن، متن بلند، عدد، تاریخ، بله/خیر و فهرست به پرونده اضافه شد. نمودار دندان FDI برای دندان‌های دائمی و شیری، سطح‌های دندان، وضعیت‌های بالینی و تاریخچهٔ append-only اضافه شد. طرح درمان اکنون چند آیتم دارد و هر آیتم به مرحله، خدمت، دندان/سطح، اولویت، هزینه و تاریخ برنامه‌ریزی متصل می‌شود؛ تغییر وضعیت آیتم نیز تاریخچه و دلیل دارد. رابط‌های فارسی RTL، ناوبری مجوزمحور و تست‌های نقش/جداسازی Tenant به‌روزرسانی شدند.

Quality Gate: ۳۴ تست موفق، ۱۲۰ assertion، Pint روی ۱۲۵ فایل، Composer audit بدون advisory، View cache، Migration fresh/seed، Vite build و `git diff --check` موفق.

## نسخهٔ 0.1.5 — 2026-08-12

فرم ورود اصلاح شد تا نام کاربری به‌درستی از شمارهٔ موبایل تفکیک و نرمال شود. برای فیلدهای الزامی فرم ورود پیام‌های فارسی صریح اضافه شد تا حتی در صورت تنظیم نادرست Locale یا نبود بستهٔ ترجمه، کلید خامی مانند `validation.required` به کاربر نمایش داده نشود. تست ورود با موبایل، ورود با نام کاربری و پیام اعتبارسنجی فارسی اضافه شد.

## نسخهٔ 0.1.4 — 2026-08-12

نصب‌کنندهٔ `install-laragon.bat` به جریان خودکار و تکرارپذیر ارتقا یافت. اسکریپت اکنون `.env`، APP_KEY، SQLite، مسیرهای runtime، Composer، Migration، cache و Vite را کنترل و آماده می‌کند. در دریافت Git وابستگی‌های Frontend از روی lockfile نصب و assetهای `public/build/manifest.json` ساخته می‌شوند؛ بنابراین خطای `ViteManifestNotFoundException` رفع می‌شود. در پایان، بر اساس وجود lock نصب، مرورگر به `/install` یا `/login` هدایت می‌شود.

## نسخهٔ 0.1.3 — 2026-08-12

اسکریپت `install-laragon.bat` اصلاح شد تا مسیرهای runtime موردنیاز Laravel، به‌ویژه `storage/framework/views`، پیش از اجرای Artisan ساخته شوند. همچنین ترتیب نصب به‌گونه‌ای تغییر کرد که Migration پیش از `optimize:clear` اجرا شود؛ به این ترتیب خطای `View path not found` و وابستگی زودهنگام cache به جدول‌های database در نصب محلی برطرف می‌شود.

## نسخهٔ 0.1.2 — 2026-08-12

اسکریپت Release تمیز اصلاح شد تا فایل‌های cache PHP محیط توسعه، از جمله manifestهای وابسته به Laravel Pail، وارد ZIP عمومی نشوند؛ در عین حال `bootstrap/cache` و مسیرهای runtime موردنیاز Laravel به‌صورت ساختار خالی باقی می‌مانند. Smoke Test از ZIP استخراج‌شده با نصب `composer --no-dev`، تولید APP_KEY، package discovery، migration، پاک‌سازی cache و اجرای Laravel 12.66.0 با موفقیت کامل شد. ترتیب دستورات نصب Laragon و cPanel نیز برای اجرای Migration پیش از `optimize:clear` همگام شد.

## نسخهٔ 0.1.0 — 2026-08-12

این نسخه اسکلت Laravel 12، ویزارد نصب، احراز هویت مبتنی بر موبایل/نام کاربری، نقش‌ها و مجوزهای Tenant، مدیریت کلینیک و شعبه، Audit، QR بیمار، صف تأیید، پروندهٔ پایه، حساسیت و دارو، تقویم هفتگی شمسی، نوبت‌دهی با کنترل هم‌پوشانی، مراحل و طرح درمان، اعلان داخلی، مدیریت پزشک/منشی، فاکتور/پرداخت و تنظیم فونت آفلاین را اضافه می‌کند.

در این نسخه ۲۸ تست و ۹۵ assertion موفق، Laravel Pint روی ۱۱۱ فایل، Blade view cache، Composer validation/audit و Vite build اجرا شده است. نسخه برای staging و بررسی cPanel آماده است؛ استفادهٔ Production با دادهٔ واقعی منوط به اتصال MySQL، HTTPS، APP_DEBUG=false، بکاپ، Cron و Smoke Test هاست است.

### Backlog

نمودار گرافیکی دندان، Custom Field کامل، فایل پزشکی، نسخه‌نویسی، SMS/IPPanel، Email، درگاه پرداخت، پورتال بیمار، گزارش‌ساز، Queue/Worker تولیدی، backup/restore UI و تست بار کنترل‌شده در نسخه‌های بعدی تکمیل خواهند شد.


## اصلاحیهٔ 0.1.1 — 2026-08-12

خطای نصب در حالتی که `vendor` ناقص است مستندسازی و رفع شد. فایل `install-laragon.bat` اضافه شد تا ابتدا وابستگی‌های کامل Laravel با `composer install --no-scripts` نصب شوند و سپس autoload، package discovery، cache و migration اجرا شوند. راهنمای فارسی `docs/operations/laragon-install-fa.md` نیز اضافه شد. نصب تمیز بدون vendor در محیط ایزوله با موفقیت تأیید شد.


## نسخهٔ انتشار تمیز — 2026-08-12

سیاست رسمی Release اضافه شد: بستهٔ عمومی بدون `vendor`، `node_modules`، `.env`، Log، Cache، دادهٔ محلی و فایل‌های توسعه ساخته می‌شود. README گیت‌هاب، INSTALL و راهنماهای cPanel با SSH و بدون SSH بازنویسی شدند. اسکریپت `scripts/build-clean-release.sh` برای ساخت ZIP تکرارپذیر و کنترل SHA256 اضافه شد.
