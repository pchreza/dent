@extends('layouts.app', ['title' => 'افزودن کاربر کلینیک'])

@section('content')
<div class="page-header"><div><span class="eyebrow">{{ $tenant->name }}</span><h1>افزودن پزشک یا منشی</h1><p class="muted">کاربر با رمز اولیه ساخته می‌شود و در اولین ورود باید رمز را تغییر دهد.</p></div><a class="button button--ghost" href="{{ route('clinic-users.index') }}">بازگشت</a></div>
<section class="card card--wide"><form method="post" action="{{ route('clinic-users.store') }}" class="form-grid" novalidate>@csrf
<div class="field-grid"><div class="field"><label for="name">نام و نام خانوادگی <span aria-hidden="true">*</span><span class="sr-only"> الزامی</span></label><input id="name" name="name" value="{{ old('name') }}" required></div><div class="field"><label for="mobile">موبایل <span aria-hidden="true">*</span><span class="sr-only"> الزامی</span></label><input id="mobile" name="mobile" value="{{ old('mobile') }}" required dir="ltr" inputmode="tel"></div><div class="field"><label for="username">نام کاربری <span aria-hidden="true">*</span><span class="sr-only"> الزامی</span></label><input id="username" name="username" value="{{ old('username') }}" required dir="ltr"></div><div class="field"><label for="role">نقش <span aria-hidden="true">*</span><span class="sr-only"> الزامی</span></label><select id="role" name="role" required><option value="doctor" @selected(old('role') === 'doctor')>پزشک</option><option value="receptionist" @selected(old('role') === 'receptionist')>منشی</option></select></div><div class="field"><label for="password">رمز اولیه <span aria-hidden="true">*</span><span class="sr-only"> الزامی</span></label><input id="password" type="password" name="password" required minlength="10" dir="ltr" autocomplete="new-password"></div><div class="field"><label for="password_confirmation">تکرار رمز <span aria-hidden="true">*</span><span class="sr-only"> الزامی</span></label><input id="password_confirmation" type="password" name="password_confirmation" required minlength="10" dir="ltr" autocomplete="new-password"></div><div class="field"><label for="license_no">شماره نظام پزشکی</label><input id="license_no" name="license_no" value="{{ old('license_no') }}" dir="ltr"></div><div class="field"><label for="specialty">تخصص</label><input id="specialty" name="specialty" value="{{ old('specialty') }}"></div></div>
<div class="form-actions"><button class="button button--primary" type="submit">ساخت کاربر</button></div></form></section>
@endsection
